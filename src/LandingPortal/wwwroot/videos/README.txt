Drop the campaign videos here:

  video1.mp4   (used by Index, Combined, CivicDuty by default)
  video2.mp4   (used by Enforcement by default)

You can also override the per-campaign video by setting CampaignMaster.VideoUrl
in SQL — the views fall back to /videos/videoN.mp4 only when VideoUrl is null.
